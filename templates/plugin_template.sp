#include <sdkhooks>
#include <sdktools>
#include <sourcemod>
#pragma newdecls required
#pragma semicolon 1


public Plugin myinfo =
{
	name = "${plugin_name}",
	author = "${AuthorName}",
	description = "",
	version = "1.0.0",
	url = "https://github.com/${GithubName}/${plugin_name}"
};


public void OnPluginStart()
{
	
}