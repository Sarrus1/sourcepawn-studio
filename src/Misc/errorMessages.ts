export const errorDetails = {
  "001": "A required token is omitted.",
  "002": "Every case in a switch statement can hold exactly one statement. To put multiple statements in a case, enclose these statements between braces (which creates a compound statement).",
  "003": "The declaration of a local variable must appear between braces (“{. . .}”) at the active scope level. When the parser flags this error, a variable declaration appears as the only statement of a function or the only statement below an if, else, for, while or do statement. Note that, since local variables are accessible only from (or below) the scope that their declaration appears in, having a variable declaration as the only statement at any scope is useless.",
  "004": "There is no implementation for the designated function. The function may have been “forwardly” declared —or prototyped— but the full function definition including a statement, or statement block, is missing.",
  "005": "The function main is the program entry point. It may not have arguments.",
  "006": "String literals or arrays must be assigned to an array. This error message may also indicate a missing index (or indices) at the array on the right side of the “=” sign.",
  "007": "Only a select set of operators may be redefined, this operator is not one of them. See page 84 for details.",
  "008": "The size of arrays and the parameters of most directives must be constant values.",
  "009": "The number of elements of an array must always be 1 or more. In addition, an array that big that it does exceeds the range of a cell is invalid too.",
  "010": "The compiler expects a declaration of a global variable or of a function at the current location, but it cannot interpret it as such.",
  "011": "The instruction or statement is invalid at a global level. Local labels and (compound) statements are only valid if used within functions.",
  "012": "The symbol is not a function.",
  "013": "The file does not contain a main function or any public function. The compiled file thereby does not have a starting point for the execution.",
  "014": "The statements case and default are only valid inside a switch statement.",
  "015": "'Default' must be the last clause in switch statement pawn requires the default clause to be the last clause in a switch statement.",
  "016": "Each switch statement may only have one default clause.",
  "017": "The symbol (variable, constant or function) is not declared.",
  "018": "Initialization: 63\nAn array with an explicit size is initialized, but the number of initiallers exceeds the number of elements specified. For example, in “arr[3]={1,2,3,4};” the array is specified to have three elements, but there are four initiallers.",
  "019": "A goto statement branches to a symbol that is not a label.",
  "020": "A symbol may start with a letter, an underscore or an “at” sign (“@”) and may be followed by a series of letters, digits, underscore characters and “@” characters.",
  "021": "The symbol was already defined at the current level.",
  "022": "The symbol that is altered (incremented, decremented, assigned a value, etc.) must be a variable that can be modified (this kind of variable is called an lvalue). Functions, string literals, arrays and constants are no lvalues. Variables declared with the “const” attribute are no lvalues either.",
  "023": "When assigning one array to another, you cannot combine an arithmetic operation with the assignment (e.g., you cannot use the “+=” operator).",
  "024": "The statements break and continue are only valid inside the context of a loop (a do, for or while statement). Unlike the languages C/C++ and Java, break does not jump out of a switch statement.",
  "025": "The number of arguments given at a previous declaration of the function does not match the number of arguments given at the current declaration.",
  "026": "The directive #else or #endif was encountered, but no matching #if directive was found.",
  "027": "One likely cause for this error is the occurrence of an unknown escape sequence, like “\\x”. Putting multiple characters between single quotes, as in ’abc’ also issues this error message. A third cause for this error is a situation where a character constant was expected, but none (or a non-character expression) were provided.",
  "028": "The subscript operators “[” and “]” are only valid with arrays. The number of square bracket pairs may not exceed the number of dimensions of the array.",
  "029": "The compiler could not interpret the expression.",
  "030": "An unexpected end of file occurred. One or more compound statements are still unfinished (i.e. the closing brace “}” has not been found). The line number where the compound statement started is given in the message.",
  "031": "The character “#” appears first at a line, but no valid directive was specified.",
  "032": "The array index is larger than the highest valid entry of the array.",
  "033": "An array as a whole cannot be used in a expression; you must indicate an element of the array between square brackets.",
  "034": "You can only use the argument placeholder when the function definition specifies a default value for the argument.",
  "035": "The argument that you pass is different from the argument that the function expects, and the compiler cannot convert the passed-in argument to the required type. For example, you cannot pass the literal value “1” as an argument when the function expects an array or a reference.",
  "036": "The line contains a semicolon that is not preceded by an expression. pawn does not support a semicolon as an empty statement, use an empty compound block instead.",
  "037": "A string was not well-formed; for example, the final quote that ends a string is missing, or the filename for the #include directive was not enclosed in double quotes or angle brackets.",
  "038": "There were trailing characters on a line that contained a directive (a directive starts with a # symbol, see page 116).",
  "039": "A variable has a size (measured in a number of cells), a constant has no size. That is, you cannot use a (symbolic) constant with the sizeof operator, for example.",
  "040": "A preceding “case label” in the list of the switch statement evaluates to the same value.",
  "041": "You used a syntax like “arr[] = { 1, ... };”, which is invalid, because the compiler cannot deduce the size of the array from the declaration.",
  "042": "A function or variable is denoted as both “public” and “native”, which is unsupported. Other combinations may also be unsup- ported; for example, a function cannot be both “public” and “stock” (a variable may be declared both “public” and “stock”).",
  "043": "When the error occurs on a literal string, it is usually an attempt to store a Unicode character in a packed string where a packed character is 8-bits. For a literal array, one of the constants does not fit in the range for packed characters.",
  "044": "When you mix positional parameters and named parameters in a function call, the positional parameters must come first.",
  "045": "The maximum number of function arguments is currently limited to 64.",
  "046": "For array assignment, the size of both arrays must be explicitly defined, also if they are passed as function arguments.",
  "047": "For array assignment, the arrays on the left and the right side of the assignment operator must have the same number of dimensions. In addition:\n- for multi-dimensional arrays, both arrays must have the same size \n—note that an unpacked array does not fit in a packed array with the same number of elements;\n- for single arrays with a single dimension, the array on the left side of the assignment operator must have a size that is equal or bigger than the one on the right side.When passing arrays to a function argument, these rules also hold for the array that is passed to the function (in the function call) versus the array declared in the function definition.When a function returns an array, all return statements must specify an array with the same size and dimensions.",
  "048": "For an array assignment, the dimensions of the arrays on both sides of the “=” sign must match; when passing arrays to a function argument, the arrays passed to the function (in the function call) must match with the definition of the function arguments. When a function returns an array, all return statements must specify an array with the same size and dimensions.",
  "049": "A line continuation character (a backslash at the end of a line) is at an invalid position, for example at the end of a file or in a single line comment.",
  "050": "A numeric range with the syntax “n1 .. n2”, where n1 and n2 are numeric constants, is invalid. Either one of the values in not a valid number, or n1 is not smaller than n2.",
  "051": "You can use the “character array index” operator (braces: “{ }” only for the last dimension, and only when indexing the array with a number. For other dimensions, and when indexing the array with a “symbolic index” (one that starts with a “.”), you must use the cell index operator (square brackets: “[ ]”).",
  "052": "If an array with more than one dimension is initialized at its declaration, then there must be equally many literal vectors/sub- arrays at the right of the equal sign (“=”) as specified for the major dimension(s) of the array.",
  "053": "The current implementation of the pawn compiler only supports arrays with one or two dimensions.",
  "054": "A closing brace (“}”) was found without matching opening brace (“{”).",
  "055": "An opening brace (“{”) was found outside the scope of a function. This may be caused by a semicolon at the end of a preceding function header.",
  "056": "A local variable or a function argument starts with the character “@”, which is invalid.",
  "057": "Compiler directives may only occur between statements, not inside a statement. This error typically occurs when an expression statement is split over multiple lines and a compiler directive appears between the start and the end of the expression. This is not supported.",
  "058": "In the function call, the same argument appears twice, possibly through a mixture of named and positional parameters.",
  "059": "All arguments of public functions must be passed explicitly. Public functions are typically called from the host application, who has no knowledge of the default parameter values. Arguments of user defined operators are implied from the expression and cannot be inferred from the default value of an argument.",
  "060": "Two or more #else directives appear in the body between the matching #if and #endif.",
  "061": "All #elseif directives must appear before the #else directive. This error may also indicate that an #endif directive for a higher level is missing.",
  "062": "When redefining an operator, the number of operands that the operator has (1 for unary operators and 2 for binary operators) must be equal to the number of arguments of the operator function.",
  "063": "Logical and relational operators are defined as having a result that is either true (1) or false (0) and having a “bool:” tag. A user defined operator should adhere to this definition.",
  "064": "One cannot define operators to work on untagged values, for example, because pawn already defines this operation.",
  "065": "In a user defined operator, a function argument may not have multiple tags.",
  "066": "In a user defined operator, all arguments must be cells (non-arrays) that are passed “by value”.",
  "067": "A function argument may be denoted as a “reference” or as an array, but not as both.",
  "068": "The precision was negative or too high. For floating point rational numbers, the precision specification should be omitted.",
  "069": "This #pragma conflicts with an earlier #pragma that specified a different format.",
  "070": "A rational literal number was encountered, but the format for rational numbers was not specified.",
  "071": "Like a variable, a user-defined operator must be declared before its first use. This message indicates that prior to the declaration of the user-defined operator, an instance where the operator was used on operands with the same tags occurred. This may either indicate that the program tries to make mixed use of the default operator and a user-defined operator (which is unsupported), or that the user-defined operator must be “forwardly declared”.",
  "072": "You used something like “sizeof MyCounter” where the symbol “MyCounter” is not a variable, but a function. You cannot request the size of a function.",
  "073": "The function argument is a constant or a simple variable, but the function requires that you pass an array.",
  "074": "Any pattern for the #define directive must start with a letter, an underscore (“_”) or an “@”-character. The pattern is the first word that follows the #define keyword.",
  "075": "Either the source file contains a very long line, or text substitutions make a line that was initially of acceptable length grow beyond its bounds. This may be caused by a text substitution that causes recursive substitution (the pattern matching a portion of the replacement text, so that this part of the replacement text is also matched and replaced, and so forth).",
  "076": "The expression statement was not recognized as a valid statement (so it is a “syntax error”). From the part of the string that was parsed, it looks as if the source line contains a function call in a “procedure call” syntax (omitting the parentheses), but the function result is used —assigned to a variable, passed as a parameter, used in an expression. . .",
  "077": "The file starts with an UTF-8 signature, but it contains encodings that are invalid UTF-8. If the source file was created by an editor or converter that supports UTF-8, the UTF-8 support is non-conforming.",
  "078": "The function returns both with and without a return value. The function should be consistent in always returning with a function result, or in never returning a function result.",
  "079": "The function returns both values and arrays, which is not allowed. If a function returns an array, all return statements must specify an array (of the same size and dimensions).",
  "080": "Where a constant value was expected, an unknown symbol or a non-constant symbol (variable) was found.",
  "082": "Only standard and public functions may have states.",
  "083": "There are multiple automatons in the state declaration for the indicated function or variable, which is not supported. In the case of a function: all instances of the function must belong to the same automaton. In the case of a variable: it is allowed to have several variables with the same name belonging to different automatons, but only in separate declarations —these are distinct variables.",
  "084": "The specified state appears in the state specifier of two implemen- tations of the same function.",
  "085": "When this error occurs on a function, this function has a fall-back implementation, but no other states. If the error refers to a variable, this variable does not have a list of states between the < and > characters. Use a state-less function or variable instead.",
  "086": "The “state” statement refers to an unknown automaton.",
  "087": "The “state” statement refers to an unknown state (for the specified automaton).",
  "088": "Only standard (global) variables may have a list of states (and an automaton) at the end of a declaration.",
  "089": "Variables with a state list may not have initializers. State variables should always be initialized through an assignment (instead of at their declaration), because their initial value is indeterminate.",
  "090": "A public function may not return an array. Returning arrays is allowed only for normal functions.",
  "091": "The first constant in a list of enumerated symbolic constants must be set to a value. Any subsequent symbol is automatically set the the value of the preceding symbol +1.",
  "092": "A symbol started with a digit, but is is not a valid number.",
  "093": "In the final dimension (the “minor” dimension), the fields of an array may optionally be declared with a size that is different from a single cell. On the major dimensions of an array, this is not valid, however.",
  "094": "Either the array was declared with symbolic subscripts and you are indexing it with an expression, or you are indexing the array with a symbolic subscript which is not defined for the array. • Fatal Errors",
  "100": "The compiler cannot find the specified file or does not have access to it.",
  "101": "The compiler cannot write to the specified output file, probably caused by insufficient disk space or restricted access rights (the file could be read-only, for example).",
  "102": "An internal table in the pawn parser is too small to hold the required data. Some tables are dynamically growable, which means that there was insufficient memory to resize the table. The “table name” is one of the following: “staging buffer”: the staging buffer holds the code generated for an expression before it is passed to the peephole optimizer. The staging buffer grows dynamically, so an overflow of the staging buffer basically is an “out of memory” error. “loop table”: the loop table is a stack used with nested do, for, and while statements. The table allows nesting of these statements up to 24 levels. “literal table”: this table keeps the literal constants (numbers, strings) that are used in expressions and as initiallers for arrays. The literal table grows dynamically, so an overflow of the literal table basically is an “out of memory” error. “compiler stack”: the compiler uses a stack to store temporary information it needs while parsing. An overflow of this stack is probably caused by deeply nested (or recursive) file inclusion. The compiler stack grows dynamically, so an overflow of the compiler stack basically is an “out of memory” error. “option table”: in case that there are more options on the command line or in the response file than the compiler can cope with.",
  "103": "General “out of memory” error.",
  "104": "Two option that are passed to the pawn compiler conflict with each other, or an option conflicts with the configuration of the pawn compiler.",
  "105": "A numeric constant, notably a dimension of an array, is too large for the compiler to handle. For example, when compiled as a 16-bit application, the compiler cannot handle arrays with more than 32767 elements.",
  "106": "(number bytes) The memory size for the abstract machine that is needed to run the script exceeds the value set with #pragma amxlimit. This means that the script is too large to be supported by the host. You might try reducing the script’s memory requirements by:,\n- setting a smaller stack/heap area —see #pragma dynamic at page 119;\n- using packed strings instead of unpacked strings —see pages 99 and 136;\n- using overlays —see pages 120 and page 170 for more information on overlays.\n- putting repeated code in separate functions;\n- putting repeated data (strings) in global variables;\n- trying to find more compact algorithms to perform the same task.",
  "107": "A single line that causes several error/warning messages is often an indication that the pawn parser is unable to “recover” from an earlier error. In this situation, the parser is unlikely to make any sense of the source code that follows —producing only (more) inappropriate error messages. Therefore, compilation is halted.",
  "108": "The file for the codepage translation that was specified with the -c compiler option or the #pragma codepage directive could not be loaded.",
  "109": "A path, for example for include files or codepage files, is invalid. Check the compiler options and, if used, the configuration file.",
  "110": "Compile-time assertion failed.",
  "111": "The parser fell on an #error directive.",
  "112": "The size of a function is too large for the overlay system. To fix this issue, you will have to split the function into two (or more) functions. \n• Warnings",
  "200": "The symbol is longer than the maximum symbol length. The maximum length of a symbol depends on whether the symbol is native, public or neither. Truncation may cause different symbol names to become equal, which may cause error 021 or warning 219.",
  "201": "The symbol was previously defined to a different value, or the text substitution macro that starts with the prefix name was redefined with a different substitution text.",
  "202": "At a function call, the number of arguments passed to the function (actual arguments) differs from the number of formal arguments declared in the function heading. To declare functions with variable argument lists, use an ellipsis (...) behind the last known argument in the function heading; for example: print(formatstring,...); (see page 78).",
  "203": "A symbol is defined but never used. Public functions are excluded from the symbol usage check (since these may be called from the outside).",
  "204": "A value is assigned to a symbol, but the contents of the symbol are never accessed.",
  "205": "Where a conditional expression was expected, a constant expression with the value zero was found, e.g. “while (0)” or “if (0)”. The the conditional code below the test is never executed, and it is therefore redundant.",
  "206": "Where a conditional expression was expected, a constant expression with a non-zero value was found, e.g. if (1). The test is redundant, because the conditional code is always executed. To create an endless loop, use for ( ;; ) instead of while (1).",
  "207": "The compiler ignores the pragma. The #pragma directives may change between compilers of different vendors and between different versions of a compiler of the same version.",
  "208": "reparse \nWhen a function is “used” (invoked) before being declared, and that function returns a value with a tag name, the parser must make an extra pass over the source code, because the presence of the tag name may change the interpretation of operators (in the presence of user-defined operators). You can speed up the parsing/compilation process by declaring the relevant functions before using them.",
  "209": "The function does not have a return statement, or it does not have an expression behind the return statement, but the function’s result is used in a expression.",
  "210": "A local (uninitialized) variable appears to be read before a value is assigned to it. The compiler cannot determine the actual order of reading from and storing into variables and bases its assumption of the execution order on the physical appearance order of statements an expressions in the source file.",
  "211": "Where a conditional expression was expected, the assignment operator (=) was found instead of the equality operator (==). As this is a frequent mistake, the compiler issues a warning. To avoid this message, put parentheses around the expression, e.g. if ( (a=2) ).",
  "212": "Where a conditional expression was expected, a bitwise operator (& or |) was found instead of a Boolean operator (&& or ||). In situations where a bitwise operation seems unlikely, the compiler issues this warning. To avoid this message, put parentheses around the expression.",
  "213": "A tag mismatch occurs when:\n- assigning to a tagged variable a value that is untagged or that has a different tag\n- the expressions on either side of a binary operator have different tags\n- in a function call, passing an argument that is untagged or that has a different tag than what the function argument was defined with\n- indexing an array which requires a tagged index with no tag or a wrong tag name",
  "214": "Arrays are always passed by reference. If a function does not modify the array argument, however, the compiler can sometimes generate more compact and quicker code if the array argument is specifically marked as “const”.",
  "215": "The result of the expression is apparently not stored in a variable or used in a test. The expression or expression statement is therefore redundant.",
  "216": "pawn does not support nested comments.",
  "217": "Statements at the same logical level do not start in the same column; that is, the indents of the statements are different. Although pawn is a free format language, loose indentation frequently hides a logical error in the control flow.\nThe compiler can also incorrectly assume loose indentation if the tab size with which you indented the source code differs from the assumed size. This may happen if the source files use a mixture of tab and space characters to indent lines. Sometimes it is then needed to tell the pawn parser what tab size to use, see #pragma tabsize on page 121 or the compiler option -t on page 170.\nYou can also disable this warning with #pragma tabsize 0 or the compiler option -t:",
  "218": "When using “optional semicolons”, it is preferred to explicitly declare forward functions with the forward keyword than using terminating semicolon.",
  "219": "A local variable has the same name as a global variable, a function, a function argument, or a local variable at a lower precedence level. This is called “shadowing”, as the new local variable makes the previously defined function or variable inaccessible.\nNote: if there are also error messages further on in the script about missing variables (with these same names) or brace level problems, it could well be that the shadowing warnings are due to these syntactical and semantical errors. Fix the errors first before looking at the shadowing warnings.",
  "220": "In a case statement and in expressions in the conditional operator (“ ? : ”), any expression that has a tag override should be en- closed between parentheses, to avoid the colon to be misinterpreted as a separator of the case statement or as part of the conditional operator.",
  "221": "A code label (for the goto instruction) has the same name as a previously defined tag. This may indicate a faultily applied tag override; a typical case is an attempt to apply a tag override on the variable on the left of the = operator in an assignment statement.",
  "222": "A literal rational number has more decimals in its fractional part than the precision of a rational number supports. The remaining decimals are ignored.",
  "223": "A function argument has a as its default value the size of another argument of the same function. The “sizeof” default value is only useful when the size of the referred argument is unspecified in\nthe declaration of the function; i.e., if the referred argument is an array.",
  "224": "The operand of the sizeof operator is an array with an unspecified size. That is, the size of the variable cannot be determined \nat compile time. If used in an “if” instruction, consider a conditionally compiled section, replacing if by #if.",
  "225": "The indicated code will never run, because an instruction before (above) it causes a jump out of the function, out of a loop or elsewhere. Look for return, break, continue and goto instructions above the indicated line. Unreachable code can also be caused by an endless loop above the indicated line.",
  "226": "There is a statement like “x = x” in the code. The parser checks for self assignments after performing any text and constant substitutions, so the left and right sides of an assignment may appear to be different at first sight. For example, if the symbol “TWO” is a constant with the value 2, then “var[TWO] = var[2]” is also a self-assignment.\nSelf-assignments are, of course, redundant, and they may hide an error (assignment to the wrong variable, error in declaring constants).\nNote that the pawn parser is limited to performing “static checks” only. In this case it means that it can only compare array assignments for self-assignment with constant array indices.",
  "227": "An array that is declared with sumbolic subscripts contains more values/fields as initiallers than there are (symbolic) subscripts.",
  "228": "The initialler for an array element contains more values than the size of that field allows. This occurs in an array that has symbolic subscripts, and where a particular subscript is declared with a size.",
  "229": "An array is declared as packed (with { and } braces) but indexed as unpacked (with [ and ]), or vice versa. Or one array is assigned to another and one is packed while the other is unpacked.",
  "230": "A function is lacking an implementation for the indicated state. The compiler cannot (statically) check whether the function will ever be called in that state, and therefore it issues this warning. When the function would be called for the state for which no implementation exists, the abstract machine aborts with a run time error.\nSee page 81 on how to specify a fall-back function, and page 42 for a description and an example.",
  "231": "A state specification is redundant on forward declarations. The function signature must be equal for all states. Only the imple- mentations of the function are state-specific.",
  "232": "The pawn compiler was configured with predefined indices for native functions, but it encountered a declaration for which it does not have an index declaration. This usually means that the script uses include files that arenot appropriate for the active configuration.",
  "233": "The state variable has the same name as a global variable (without state specifiers). This means that the global variable is inaccessible for a function with one of the same states as those of the variable.",
  "234": "The script uses a function which is marked as “deprecated”. The host application can mark (native) functions as deprecated when better alternatives for the function are available or if the function may not be supported in future versions of the host application.",
  "235": "The script defines a public function, but no forward declaration of this function is present. Possibly the function name was written incorrectly. The requirement for forward declarations of public functions guards against a common error.",
  "236": "A #define pattern contains a parameter in the replacement (e.g. “%1”), that is not in the match pattern. See page 92 for the preprocessor syntax.",
  "237": "The specified function calls itself recursively. Although this is valid in pawn, a self-call is often an error. Note that this warning is only generated when the pawn parser/compiler is set to “verbose” mode.",
  "238": "In concatenating literals strings, strings with different formats (such as packed versus unpacked, and “plain” versus standard strings) were combined. This is usually an error. The parser uses the format of the first (left-most) string in the concatenation for the result."
}
